Makefile:
	reecrire *.c des sources en dur

fonctioni free_param 
	ex :  check_error
Malloc / free :
	get_input
		input = read_line // free main

	init_param
		param  = (t_data *)malloc(sizeof(t_data));
		param->envp = copy_env(envp, 0);

	copy_env
		cpy = (char **)ft_calloc(sizeof(char *), (len + add + 1));
		cpy[i] = ft_strdup(envp[i]);
		
		
		
		
		https://github.com/lyutsiya91/pipex


	

		

• Gérer ctrl-C, ctrl-D et ctrl-\ qui doivent fonctionner comme dans bash.


• En mode interactif :
◦ ctrl-C affiche un nouveau prompt sur une nouvelle ligne.
◦ ctrl-D quitte le shell.
◦ ctrl-\ ne fait rien.
	


bash : ctrl \ sur prompt vide : rien
				promt en cours : rien
				sur cat -e : message + prompt
